base: roblox
globals:
  require:
    args:
      - type: string
  # We are theoretically not sure whether this part is needed, but there is practically no overhead and it can't hurt
  __DEV__:
    type: boolean
    property: read-only